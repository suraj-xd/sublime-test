#include <bits/stdc++.h>
using namespace std;
const int N = 100;
int dp[N];
int timer = 0;
// solve by brute force

// try to find out sub problems (base cases)

// save the answer

// or evaluate

// return if already found.

int main()
{   
    for(int i = 0;i < 100;i++){
        dp[i] = -1;
    }
    fib(10);
}