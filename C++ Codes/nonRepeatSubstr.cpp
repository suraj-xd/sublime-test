#include <bits/stdc++.h>
using namespace std;
int main(){
    // max no repeating substring


    // string s; cin>>s;
    // vector<int> dict(256,-1);
    // int maxlen = 0, start = -1;
    // for (size_t i = 0; i < s.size(); i++)
    // {
    //     if(dict[s[i]]>start){
    //         start = dict[s[i]];
    //     }
    //     dict[s[i]] = i;
    //     maxlen = max(maxlen,i-start);
    // }
    
    




    // string a = "abcabccab";
    // cout<<(a[0]!=a[0+1]);
    // return 0;
    // int maximum = 0, curr = 0;
    // for (size_t i = 0; i < 8; i++)
    // {
    //     if(a[i]!=a[i+1]){
    //         curr++;
    //         maximum = max(maximum,curr);
    //     }else{
    //         curr=0;
    //     }
    // }
    // cout<<maximum;
}